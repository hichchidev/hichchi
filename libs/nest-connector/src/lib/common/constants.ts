export const SECOND_IN_MS = 1000;
export const MINUTE_IN_SECONDS = 60;
export const HOUR_IN_MINUTES = 60;
export const DAY_IN_HOURS = 24;
export const DAY_IN_SECONDS = DAY_IN_HOURS * HOUR_IN_MINUTES * MINUTE_IN_SECONDS;
export const MONTH_IN_DAYS = 30;
export const MONTH_IN_SECONDS = MONTH_IN_DAYS * DAY_IN_SECONDS;

export const DEFAULT_VERIFY_TOKEN_LENGTH = 16;

export const DEFAULT_SALT_ROUNDS = 10;

export const DEFAULT_UUID_VERSION = 4;

export const DEFAULT_REDIS_PORT = 6379;

export const DEFAULT_MYSQL_PORT = 3306;

export const DEFAULT_LOG_LEVEL = 4;

export const DEFAULT_JSON_SPACE = 2;

export const DEFAULT_LOG_FILE_LENGTH = 100;

export const DEFAULT_ITEMS_PER_PAGE = 10;
